public int M = 32; 

public int main()
{
	private int a, b, c; 
	smcinput(a, 1); 
	public int i; 

	for(i = 0; i < 0.9 * M; i++) 
		b = a * a;
	for(i = 0; i < 0.1 * M; i++)
		c = a + a; 

	smcoutput(b, 1);
	smcoutput(c, 1);
	return 0; 
}
