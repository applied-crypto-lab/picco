public int K=1000000;
public int main()
{
	public int i;
	private int A[K], B[K]; 
	smcinput(A, 1, K);

	for(i = 0; i < K-1; i++)[
		B[i] = A[i] * A[i+1]; 
    ]

    smcoutput(B, 1, K);

    return 0;
}